Stabilization Report — Issues Found
Correctness Issues
1. CoachingProfile still contains recommendedLoanTypes and qualitative fields

server/services/coachingService.ts line 14: recommendedLoanTypes: string[] is a field on CoachingProfile
The AI prompt example (line 1124) shows "recommendedLoanTypes": ["conventional", "fha"]
The profile also includes strengths (line 13) and summary (line 11) which invite qualitative language
This contradicts the stated AI boundary: "never recommend specific loan products as decisions"
2. System prompt Section 6 (Borrower Package Builder) is stale — doesn't match the actual BorrowerPackage schema

Lines 852-934: The prompt describes a 7-section package with different section names (e.g., "HOUSEHOLD OVERVIEW," "ASSET CATEGORIES," "PROPERTY INTENT") and fields (amounts, verification tiers on every row, LTV ratio)
The actual BorrowerPackage interface (lines 50-120) uses an 11-section structure with different names ("borrowerOverview," "assetSummary," "auditTrail," etc.)
The AI receives two conflicting instructions — the Section 6 prose description AND the JSON schema template (lines 1192-1271). When they disagree, output will be unpredictable.
3. "ready_now" tier definition includes eligibility-adjacent criteria

Line 1108: "ready_now" is defined as "DTI < 43%, credit 680+, stable employment 2+ years, adequate savings"
These are underwriting eligibility thresholds, not procedural readiness. A package can be procedurally complete (all inputs collected, documents uploaded) without meeting these numbers. Conflating readiness with eligibility violates the stated compliance boundary.
4. accessLink field on assetSummary — AI cannot generate valid document URLs

The AI has no access to the document database or object storage. It cannot produce real /api/documents/{id}/download paths. It will either fabricate URLs or leave them empty every time. This field is structurally impossible for the AI to populate correctly.
Compliance Risks
5. Profile summary field has no compliance guardrail

CoachingProfile.summary (line 11) is a free-text string with no prompt constraint preventing qualitative language. The AI could write "Strong borrower with solid income" here.
6. readinessScore (0-100 number) risks being interpreted as approval likelihood

Line 10: readinessScore: number — a numeric score easily conflated with "chance of approval" by borrowers or downstream consumers, despite the prompt saying "never frame completion percentage as a probability of approval."
7. Line 446: Veteran status annotation says "VA loan eligible"

if (ctx.isVeteran) lines.push('Veteran Status: Yes (VA loan eligible)') — This is an eligibility determination. The system should say "Veteran Status: Yes" without appending eligibility conclusions.
8. Line 621: Property context prompt includes "Assess whether the user's financial profile supports this purchase price"

This instructs the AI to assess financial suitability, which is an eligibility evaluation, not a procedural intake function.
Reliability Issues
9. AI JSON parsing has no schema validation

parseCoachResponse (lines 1326-1346) does JSON.parse and then assigns fields by key presence (if (data.profile)) with no shape validation. Malformed AI output silently produces partial or incorrect data. No Zod or runtime type checking.
10. formatCurrency can silently corrupt values

Line 111: Number(val.replace(/[^0-9.-]/g, "")) — If the AI outputs something like "Not Provided" or "Insufficient Data" in a field that gets passed through formatCurrency, the regex strips all letters, Number("") returns 0, and the display shows "$0" instead of the original text. The isNaN check on line 112 would catch NaN but Number("") is 0, not NaN.
11. No error boundary on BorrowerPackageView

If any AI-generated field has an unexpected shape (array instead of string, nested object instead of flat), the component will crash with no fallback. There's no try/catch or error boundary wrapping the render.
Ambiguous or Misleading Language
12. Prompt uses "strengths" and "gaps" terminology

Lines 1121-1123 in the example JSON: "strengths": ["list of financial strengths"], "gaps": ["list of areas to improve"]
"Financial strengths" is qualitative assessment language. "Areas to improve" implies the AI is coaching toward eligibility. Should be "completed inputs" and "outstanding inputs."
13. Line 984: Stall response example uses "strong foundation"

"You've already provided [X, Y, Z] which is a strong foundation" — qualitative assessment of borrower data quality.
14. Line 976: Stall response example uses "solid progress"

"that's solid progress" — mild but still qualitative.
That's the full list. Each item is classified as correctness, compliance, reliability, or language. No new features or refactors proposed — only issues.